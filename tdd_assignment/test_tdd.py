import unittest
from validation import validation


class TestValidation(unittest.TestCase):

    def test_username(self):
        username = " "
        self.assertTrue(tdd.user_valiidation(test_username))

    def test_password(self):
        password = " "
        self.assertTrue(tdd.user_validation(password))

if __name__ == "__main__":
 while(True):
     break
     name = input("wrong ")